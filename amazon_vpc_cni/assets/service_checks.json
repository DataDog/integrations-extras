[
    {
        "agent_version": "7.59.0",
        "integration": "Amazon VPC CNI",
        "check": "amazon_vpc_cni.openmetrics.health",
        "statuses": ["ok", "critical"],
        "groups": ["host", "endpoint"],
        "name": "Amazon VPC CNI OpenMetrics endpoint health",
        "description": "Returns `CRITICAL` if the Agent is unable to connect to the Amazon VPC CNI OpenMetrics endpoint, otherwise returns `OK`."
    }
]
