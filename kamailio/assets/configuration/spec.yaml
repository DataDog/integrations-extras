name: kamailio
files:
- name: kamailio.yaml
  options:
  - template: init_config
    options:
    - template: init_config/default
  - template: instances
    options:
    - name: jsonrpc_api_url
      required: true
      description: The kamailio jsonrpc API url.
      value:
        type: string
        example: "http://127.0.0.1:5060/api/kamailio"
        default: "http://127.0.0.1:5060/api/kamailio"
    - template: instances/default