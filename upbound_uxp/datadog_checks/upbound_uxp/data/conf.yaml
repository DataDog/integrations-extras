init_config:

instances:
  [
    {
      "verbose": true,                        # print useful info when verbose is true
      "metrics_default": "max",               # select from a predefined set of metrics to collect: none, min, more, max
    }
  ]
