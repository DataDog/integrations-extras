integration_tests:
  - name: upbound_foo
    runner: agent
    env:
      KUBECONFIG: /tmp/uxp.kubeconfig
      DD_API_KEY: ${DATADOG_API_KEY}
    config_provider: file
    config_file: datadog_checks/upbound_uxp/data/auto_conf.yaml
