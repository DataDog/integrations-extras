[
  {
    "agent_version": "5.3.0",
    "integration": "Celery",
    "check": "celery.worker.ping",
    "statuses": [
      "ok",
      "critical"
    ],
    "groups": [
      "worker",
      "app"
    ],
    "name": "Worker ping response",
    "description": "Returns `OK` if the worker responded to a ping. Returns `CRITICAL` for a worker that hasn't responded to the ping, only if `remember_workers` is set to `true`."
  }
]
