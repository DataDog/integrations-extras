{
       "agent_version": "6.2.0",
       "integration":"bind9_check",
       "check": "bind9_check",
       "statuses": ["ok", "critical"],
       "description": "Returns `OK` If Statistics-channel URL of DNS is present in Instance.Returns `CRITICAL` If URL Errors occurs."
   }
