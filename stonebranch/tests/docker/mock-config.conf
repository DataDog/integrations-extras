server {
    listen 80;
    server_name localhost;

    location /resources/metrics {
        auth_basic "Stonebranch Metrics";
        auth_basic_user_file /usr/share/nginx/html/.htpasswd;
        try_files $uri $uri/ =404;
    }

    location / {
        return 404;
    }
}