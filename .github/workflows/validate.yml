name: Validate repository

on:
  push:
    branches:
    - master
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.sha }}
  cancel-in-progress: true

jobs:
  run:
    uses: DataDog/integrations-core/.github/workflows/run-validations.yml@c9d96c04c9603581becabc1a122159d42878abfa # master
    with:
      repo: extras

      # Validations
      ci: true
      config: true
      imports: true
      integration-style: true
      jmx-metrics: true
      legacy-signature: true
      metadata: true
      models: true
      package: true
      readmes: true
      codeowners: true
    secrets: inherit
