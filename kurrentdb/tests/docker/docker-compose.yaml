services:
  kurrentdb:
    image: docker.kurrent.io/kurrent-latest/kurrentdb:${KURRENTDB_VERSION:-latest}
    container_name: kurrentdb
    command: --insecure --run-projections=All --enable-atom-pub-over-http
    environment:
      KURRENTDB_CLUSTER_SIZE: "1"
    ports:
      - "2113:2113"
