[
    {
        "agent_version": "1.0.0",
        "integration":"nifi",
        "check": "nifi.instance.http_check",
        "statuses": ["ok", "warning", "critical"],
        "groups": ["host", "instance"],
        "name": "HTTP Check",
        "description": "Returns `CRITICAL` if the Agent check is unable to connect to the monitored Nifi instance. Returns `WARNING` if the attempt to connect timeout. Returns `OK` otherwise."
    }
]
