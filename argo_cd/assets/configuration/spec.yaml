name: Argo_CD
files:
- name: argo_cd.yaml
  options:
  - template: init_config
    options:
      - template: init_config/openmetrics
  - template: instances
    options:
    - template: instances/openmetrics
      overrides:
        openmetrics_endpoint.description: The URL where your Argo CD metrics are exposed by Prometheus.