init_config:

instances: 
  ## as the underlying lighthouse check can be resource intensive
  ## and take about 10 seconds per instance, this has only been tested
  ## with a min collection interval of 60 seconds for a few instances

  # - min_collection_interval: 60
  #   url: "https://docs.datadoghq.com" # required
  #   name: "datadog_docs" # required

    # optional
    # tags: 
    #   - example_tag
    #   - another_example_tag:an_example_value